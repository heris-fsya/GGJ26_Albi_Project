using UnityEngine;

public enum Operators
{
    AND,
    NAND,
    OR,
    NOR,
    XOR,
    INVERT,
    SHIFTLEFT,
    SHIFTRIGHT
}

public enum Difficulty
{
    EASY,
    MEDIUM,
    HARD,
    NONE
}
